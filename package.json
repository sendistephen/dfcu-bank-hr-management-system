{
  "name": "dfcu-bank-hr-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "yarn workspace backend start",
    "build": "yarn workspace backend build",
    "seed": "yarn workspace backend seed",
    "dev": "yarn workspace backend dev"
  },
  "workspaces": [
    "backend"
  ],
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3"
  }
}
